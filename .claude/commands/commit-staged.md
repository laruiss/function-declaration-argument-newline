# Commit Staged

Claude Code command exposing the common commit conventions [`../../.agents/commit-message.md`](../../.agents/commit-message.md).

Read this source file in full before running the command, then create a commit using only the files already staged.
